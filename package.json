{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "lerna run watch --scope @minsang/opti-ui & lerna run dev --scope playground",
    "dev-docs": "lerna run dev --scope docs",
    "build": "lerna run build --scope playground",
    "lint": "lerna run lint --scope playground",
    "preview": "lerna run preview --scope playground"
  },
  "dependencies": {
    "next": "^15.2.2",
    "nextra": "^4.2.16",
    "nextra-theme-docs": "^4.2.16",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "lerna": "^8.2.1"
  }
}
